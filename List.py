'''
                      °°LIST°°
SYLLABUS: Definition, Creation of a list, Traversal(access every element) of a list. Operations on a list - 
concatenation, repetition, membership; functions/methods–len(), list(),  append(), extend(), insert(), count(), index(), remove(), pop(), reverse(), 

sort(), min(), max(), sum(); Lists Slicing; Nested lists; finding the maximum, 

minimum, mean of numeric values stored in a list; linear search on list of 

numbers and counting the frequency of elements in a list
   _________X__________

  are four collection data types in the Python programming language:

List is a collection which is ordered and changeable. Allows duplicate members.
Tuple is a collection which is ordered and unchangeable. Allows duplicate members.
Set is a collection which is unordered and unindexed. No duplicate members.
Dictionary is a collection which is unordered, changeable and indexed. No duplicate members.

 
LIST :A list is a collection which is ordered and changeable. In Python lists are written with square brackets
'''
#list = ["a","b","c"]
#print(list)
#print(list[2]) 

              
 #TRAVERSING
l = [1,2,3]
a1=(len(l))
for i in l :
	print(i)
                                     